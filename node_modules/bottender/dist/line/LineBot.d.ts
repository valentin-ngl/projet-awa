import { LineClient } from 'messaging-api-line';
import Bot, { OnRequest } from '../bot/Bot';
import SessionStore from '../session/SessionStore';
import { LineConnectorOptions } from './LineConnector';
import LineContext from './LineContext';
import LineEvent from './LineEvent';
import { LineRequestBody } from './LineTypes';
export default class LineBot extends Bot<LineRequestBody, LineClient, LineEvent, LineContext> {
    constructor({ sessionStore, sync, onRequest, ...connectorOptions }: LineConnectorOptions & {
        sessionStore?: SessionStore;
        sync?: boolean;
        onRequest?: OnRequest;
    });
}
//# sourceMappingURL=LineBot.d.ts.map